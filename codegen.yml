schema: "src/graphql/schema.gql"
generates:
  src/graphql/types.ts:
    plugins:
      - typescript
    config:
      scalars:
        AWSPhone: string
        AWSEmail: string
        AWSURL: string
        AWSTimestamp: number